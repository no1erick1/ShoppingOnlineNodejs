<div class="container">
{{# each addproducts}}
    <div class="row equal">
    {{# each this}}
  <div class="col-sm-6 col-md-4 equal">
    <div class="thumbnail">
      <img src="{{this.imagePath}}">
      <div class="caption">
        <h3>{{this.title}}</h3>
        <p>{{this.description}}</p>
        <div class="price pull-left">{{this.price}} VND</div> 
        <a href="/add-to-cart6/{{this._id}}" class="btn btn-success pull-right" role="button">Add to cart</a></p>
      </div>
    </div>
  </div>
  {{/each}}
</div>
{{/each}}
</div>